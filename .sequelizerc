const { resolve } = require("path");

module.exports = {
  config: resolve(__dirname, "src", "config", "db", "config.js"),
  "migrations-path": resolve(__dirname, "src", "config", "db", "migrations"),
  "seeders-path": resolve(__dirname, "src", "db", "seeds")
};
